@AGENTS.md

## Development workflow

- Before starting work on any new feature, pull the latest code from `origin/Development`
  (the team's integration branch, not `main`) and branch off that up-to-date state. This
  project moves fast with many contributors landing PRs on `Development` daily, so a stale
  local checkout is the most common source of avoidable merge conflicts.
